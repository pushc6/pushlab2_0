---
# Mirror of ansible/requirements.yml — keep in sync.
# ansible-builder consumes this when building the EE image.
collections:
  - name: ansible.posix
    version: ">=2.0.0,<3.0.0"
  - name: ansible.utils
    version: ">=5.0.0,<7.0.0"
  - name: community.general
    version: ">=10.0.0,<13.0.0"
  - name: community.docker
    version: ">=4.0.0,<5.0.0"
  - name: ansibleguy.opnsense
    version: ">=1.2.0,<2.0.0"
  - name: effectivelywild.technitium_dns
    version: ">=1.0.0,<2.0.0"
