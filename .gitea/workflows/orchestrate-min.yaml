name: orchestrate-min

on:
  push:
    branches:
      - orchestrate

jobs:
  smoke:
    runs-on: [self-hosted, linux, x64, alma]
    steps:
      - name: Echo context
        run: |
          echo "orchestrate-min: ref=${{ github.ref }} sha=${{ github.sha }} runner=${{ runner.name }}"
