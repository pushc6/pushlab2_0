---
# Reverse proxy firewall settings

# Public web services (using new unified format)
firewall_services:
  - port: 80
    protocol: tcp
    name: "HTTP"
    access: public

  - port: 443
    protocol: tcp
    name: "HTTPS"
    access: public
