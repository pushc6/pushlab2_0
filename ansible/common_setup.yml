---
- name: Common Setup for All Hosts
  hosts: all
  become: yes
  gather_facts: yes

  roles:
    - system_users
    - network_interfaces
    # firewall role removed - replaced by firewall_hardened role in site.yml
