# {{ ansible_managed }}
# Runtime environment for Caddy. Consumed by the cloudflare DNS module
# ({env.CLOUDFLARE_API_TOKEN} in the Caddyfile) for ECH publication and
# DNS-01 certificate issuance. Keep this file 0600 / owned by {{ caddy_proxy_user }}.
CLOUDFLARE_API_TOKEN={{ caddy_proxy_cf_api_token }}
