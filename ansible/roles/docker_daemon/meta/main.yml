---
galaxy_info:
  author: Infrastructure Team
  description: Declaratively manage /etc/docker/daemon.json (log rotation, live-restore, host extras)
  company: Homelab
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: EL
      versions: ["9", "10"]
    - name: Ubuntu
      versions: ["all"]
  galaxy_tags:
    - docker
    - daemon

dependencies: []
collections:
  - ansible.builtin
