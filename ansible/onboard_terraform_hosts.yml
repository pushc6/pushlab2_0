---
- name: Onboard Terraform-Created Hosts
  hosts: terraform_created
  become: true
  vars:
    ansible_user: root
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"

  roles:
    - system_users
  tags: [onboard, users, terraform]
