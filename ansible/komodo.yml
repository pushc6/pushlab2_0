---
# Komodo Deployment Playbook
# Deploys Komodo container management UI

- name: Deploy Komodo
  hosts: docker-internal.localdomain
  become: true
  roles:
    - komodo
  tags: ['komodo', 'docker', 'apps']
