nginxdmz:
  hosts:
    nginx:
dockerhost:
  hosts:
    docker:
cups:
  hosts:
    cupsserver.localdomain:
homeassistant:
  hosts:
    raspberrypi-ha.localdomain:
gameserver:
  hosts:
    linuxgameserver.localdomain:
docker_internal:
  hosts:
    "docker-internal.localdomain":
docker_secure:
  hosts:
    "docker-secure.localdomain":
nginx_internal:
  hosts:
    "nginx-internal.localdomain":
crowdsec_controller:
  hosts:
    "crowdsec.localdomain":
ansible:
  hosts:
    ansible.localdomain:
prusa3dbawkz:
  hosts:
    prusa3dbawkz.localdomain:
scrypted:
  hosts:
    scrypted.localdomain:
# OPNsense removed - now in dedicated inventory: inventories/opnsense/hosts.yml
# Reason: OPNsense uses API calls (not SSH) and must never be targeted by
# general playbooks like site.yml, update_all.yml, deploy_firewall.yml, etc.

# Phased rollout groups for firewall hardening
phase1_critical:
  hosts:
    nginx:
    docker:
    "nginx-internal.localdomain":
