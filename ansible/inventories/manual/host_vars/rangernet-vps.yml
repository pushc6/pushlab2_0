---
# Configuration for rangernet-vps (192.210.235.136) - External VPS

# =============================================================================
# Tailscale Configuration
# =============================================================================
tailscale_enabled: true
tailscale_hostname: "rangernet-vps"
tailscale_tags:
  - "tag:crowdsec-agent"

# =============================================================================
# CrowdSec Configuration
# =============================================================================
crowdsec_enabled: true
crowdsec_mode: agent
crowdsec_lapi_url: "http://crowdsec.crane-beaver.ts.net:8080"
crowdsec_lapi_host: "crowdsec.localdomain"  # For bouncer registration

crowdsec_collections:
  - crowdsecurity/linux
  - crowdsecurity/nginx

crowdsec_acquis:
  - name: nginx
    filenames:
      - /var/log/nginx/access.log
      - /var/log/nginx/error.log
    labels:
      type: nginx
  - name: syslog
    filenames:
      - /var/log/messages
      - /var/log/secure
    labels:
      type: syslog

crowdsec_bouncers:
  - name: firewall
    type: nftables
    enabled: true

# =============================================================================
# Firewall Configuration
# =============================================================================

# VPS-specific: SSH must remain publicly accessible (no SSH hardening)
firewall_ssh_hardening_enabled: false

# SSH access - public VPS needs unrestricted SSH access
# (firewall_ssh_hardening_enabled: false disables rich rules, but we define this for documentation)
firewall_ssh_allowed_subnets:
  - "0.0.0.0/0"  # Public SSH access required for VPS

# Using unified firewall_services structure with rich rules
firewall_services: []
