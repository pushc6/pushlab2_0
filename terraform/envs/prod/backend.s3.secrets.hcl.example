# Secrets for S3-compatible backend (DO NOT COMMIT real values)
# Copy to backend.s3.secrets.hcl and fill in locally

# Access credentials for your S3-compatible service (e.g., Backblaze B2 S3)
access_key = "<B2_KEY_ID>"
secret_key = "<B2_APPLICATION_KEY>"

# Alternative to putting keys here:
# - Use environment variables: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY (and AWS_SESSION_TOKEN if applicable)
# - Or use a named profile in ~/.aws/credentials and set `profile` in backend.hcl
