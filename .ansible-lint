---
# Production-profile lint gate. Waves 1–5 cleared the entire skip_list.
# Variables that genuinely can't be role-prefixed (cross-role flags,
# external API contracts) carry inline `# noqa: var-naming[no-role-prefix]`
# at their declarations.
profile: production

exclude_paths:
  - .cache/
  - .git/
  # Gitea Actions workflows are not Ansible content; CI lints ansible/ only,
  # but the pre-commit hook scans the whole repo — keep the two consistent.
  - .gitea/
  - collections/
  - ansible/group_vars/vault.yml.example
  - ansible/inventories/prod/host_vars/gitea/vault.yml.example
  # Survey-driven playbook — Semaphore supplies target_host at run time.
  # This exclusion is permanent; do not attempt to default target_host
  # (would risk accidental "all hosts" runs).
  - ansible/add_ansible_user_prompt_debian.yml
